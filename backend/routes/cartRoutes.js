// import express from 'express';
// import { getCart, addItemToCart, removeItemFromCart, clearCart } from '../controllers/cartController.js';
// import { authenticate } from '../middlewares/authMiddleware.js';

// const router = express.Router();

// router.get('/', authenticate, getCart); // Get the cart
// router.post('/', authenticate, addItemToCart); // Add item to cart
// router.delete('/:productId', authenticate, removeItemFromCart); // Remove item from cart
// router.delete('/', authenticate, clearCart); // Clear cart

// export default router;
